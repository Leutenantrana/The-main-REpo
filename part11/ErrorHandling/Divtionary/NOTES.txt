(READING A FILE FROM FILES. METHODS)

1>>  List<String> lines =Files.readAllLines(Paths.get("example.txt));

         >>Files.readAllLines(Paths.get("example.txt")) reads all the lines of the file as a List<string> ;;

2>> Files.lines(Paths.get("example.txt")) reads all the lines and returns a Stream<String> type output..


3>>     try (BufferedReader reader = Files.newBufferedReader(Paths.get("example.txt"))) {
            // Read from the reader
        } catch (IOException e) {
            // Handle IOException
        }
